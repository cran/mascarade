% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{radialCandidates}
\alias{radialCandidates}
\title{Radial free-space label candidates}
\usage{
radialCandidates(
  boxfit,
  poi,
  hw,
  hh,
  pad,
  ndir,
  step,
  rstart,
  rmax,
  intfill,
  dedup
)
}
\arguments{
\item{boxfit}{External pointer from \code{buildBoxFit()}.}

\item{poi}{K x 2 matrix of cluster poles (the ray origins).}

\item{hw, hh}{Numeric per-label box half-sizes.}

\item{pad}{Numeric hard box clearance added around each box.}

\item{ndir}{Integer number of rays per pole.}

\item{step}{Numeric radial step along each ray.}

\item{rstart, rmax}{Numeric first and last radius searched.}

\item{intfill}{Numeric spacing of extra candidates along a wide free interval.}

\item{dedup}{Numeric grid size for de-duplicating nearby candidates.}
}
\value{
A data.frame with integer \code{label} (1-indexed) and numeric \code{cx}, \code{cy}.
}
\description{
From each cluster pole, marches \code{ndir} rays outward and emits a candidate box centre
wherever a box of the label's size (plus \code{pad}) is cluster-free (a BoxFit R-tree query): at
the near edge of every free interval and every \code{intfill} along wide ones. Candidates may
fall partly outside the viewport -- the effective-length overflow term ranks them, so a
crowded label can take a minimally-clipped edge slot instead of the far seed.
}
\keyword{internal}
