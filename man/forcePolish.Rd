% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{forcePolish}
\alias{forcePolish}
\title{Continuous force-directed label polish}
\usage{
forcePolish(
  boxfit,
  cx0,
  cy0,
  hw,
  hh,
  tx,
  ty,
  polysx,
  polysy,
  pad,
  xlo,
  xhi,
  ylo,
  yhi,
  iters,
  step,
  MU,
  pad_tgt,
  stepmin,
  con_type,
  sq = TRUE
)
}
\arguments{
\item{boxfit}{External pointer from \code{buildBoxFit()} (the cluster keep-out).}

\item{cx0, cy0}{Numeric starting label-centre coordinates.}

\item{hw, hh}{Numeric per-label box half-sizes.}

\item{tx, ty}{Numeric per-label pole (leader target).}

\item{polysx, polysy}{Lists of parallel numeric x/y vectors, one true mask ring per cluster
(aligned with the labels), used for the foreign-cluster arc term.}

\item{pad}{Numeric hard box clearance.}

\item{xlo, xhi, ylo, yhi}{Numeric viewport bounds.}

\item{iters}{Integer iteration count.}

\item{step}{Numeric initial pattern-search step.}

\item{MU}{Numeric weight of the box-box spacing penalty.}

\item{pad_tgt}{Numeric target inter-box spacing.}

\item{stepmin}{Numeric smallest step tried before abandoning a direction.}

\item{con_type}{Integer leader style: 0 = "ledge" (corner), otherwise "line"/"box"/"none".}

\item{sq}{Logical; if \code{TRUE}, the length term uses the squared distance.}
}
\value{
A list with numeric \code{cx}, \code{cy}: the polished label centres.
}
\description{
Pattern-search descent on the (squared) EFFECTIVE length under a hard conflict guard. The
effective length of a label is its centre-to-pole leader length, plus the arc of the leader
that runs inside any foreign cluster (routing leaders around clusters), plus a soft viewport
overflow penalty -- the same quantity the upstream \code{effectiveLength()} ranking minimises.
A box-box spacing penalty is added on top. Starting from a conflict-free layout
the search only accepts conflict-free neighbours (free-space check via the BoxFit R-tree),
so feasibility is preserved. Overflow is a SOFT term folded into the effective length, not a
hard clip, so an off-panel label can be walked back in-bounds and a label leaves the panel
only when that lowers total energy. The leader anchor rule mirrors R's \code{.anchorPoint()}, so
both the conflict guard and the foreign-cluster arc match the drawn geometry.
}
\keyword{internal}
