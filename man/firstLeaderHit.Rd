% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{firstLeaderHit}
\alias{firstLeaderHit}
\title{Visible leader end (first mask-boundary hit)}
\usage{
firstLeaderHit(ex, ey, tx, ty, polysx, polysy)
}
\arguments{
\item{ex, ey}{Numeric leader-start (anchor) coordinates, one per label.}

\item{tx, ty}{Numeric pole (leader target) coordinates, one per label.}

\item{polysx, polysy}{Lists of parallel numeric x/y vectors, one ring per label (its own
cluster).}
}
\value{
A list with numeric \code{bx}, \code{by}: the visible leader end, one per label.
}
\description{
For each label, the leader runs from its box anchor (\code{ex}, \code{ey}) to the cluster pole
(\code{tx}, \code{ty}), which lies inside the cluster. The drawn leader stops at the mask boundary:
the first point where the anchor->pole segment crosses the label's own cluster ring. When
the segment does not cross the ring the leader runs all the way to the pole.
}
\keyword{internal}
